@*@using FailTracker.Web.Infrastructure.Alerts
@if (TempData.GetAlerts().Any())
{
	<script type="text/javascript">
    $(function () {
        @foreach (var alert in TempData.GetAlerts())
			{
                <text>
        window.alerts.showAlert('@alert.Message.Replace("'","\\'")', '@alert.AlertClass');
        </text>
			}
    });
	</script>
}*@
